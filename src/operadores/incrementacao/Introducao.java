package operadores.incrementacao;

public class Introducao {
    public static void main(String[] args) {
        // ++ e --
        int contador = 0;
        contador += 1; // contador = contador +1;
        contador++; // 2
        contador--; // 1
        System.out.println(contador);
    }
}
